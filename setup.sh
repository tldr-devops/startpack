mkdir -p ${DATAPATH?Variable DATAPATH not set}/{traefik,nfs/traefik}/certificates
mkdir -p ${DATAPATH?Variable DATAPATH not set}/{mariadb,postgres,portainer,nfs/portainer,registry,nfs/registry}/data
mkdir -p ${DATAPATH?Variable DATAPATH not set}/{mariadb,postgres}/entrypoint
