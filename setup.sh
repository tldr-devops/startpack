mkdir -p ${DATAPATH?Variable DATAPATH not set}/traefik/certificates
mkdir -p ${DATAPATH?Variable DATAPATH not set}/nfs/traefik/certificates
mkdir -p ${DATAPATH?Variable DATAPATH not set}/mariadb/data
mkdir -p ${DATAPATH?Variable DATAPATH not set}/mariadb/entrypoint
mkdir -p ${DATAPATH?Variable DATAPATH not set}/postgres/data
mkdir -p ${DATAPATH?Variable DATAPATH not set}/postgres/entrypoint
mkdir -p ${DATAPATH?Variable DATAPATH not set}/portainer/data
